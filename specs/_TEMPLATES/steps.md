# Steps: [SLICE NAME]

**Spec:** [spec reference]
**Slice:** [slice reference from slices.md]
**Status:** [PENDING | IMPLEMENTED | DONE]

## Prerequisites

- [What must exist before starting]

**Verify the Prerequisites are done**

## Steps

### Backend

1. In `api/src/path/to/file.ts`, [action] [what specifically]
2. In `api/src/path/to/file.ts`, [action] following pattern in `existing-file.ts`

**REVIEW GATE: Backend complete**

### Frontend

3. In `ui/src/path/to/file.tsx`, [action] [what specifically]
4. In `ui/src/path/to/file.tsx`, [action] following existing pattern

**REVIEW GATE: Frontend complete**

### Tests

5. In `api/tests/file.test.ts`, add tests for [scenarios]

**REVIEW GATE: Tests complete**

## Verification

- [ ] [How to verify completion]

**After verification passes, mark steps as `IMPLEMENTED` and proceed to review slice.**
