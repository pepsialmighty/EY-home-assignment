server {
  listen 3000;
  root /usr/share/nginx/html;
  index index.html;

  location /api/ {
    proxy_pass https://family-tree-api.fly.dev/api/;
    proxy_ssl_server_name on;
  }

  location / {
    try_files $uri $uri/ /index.html;
  }
}
