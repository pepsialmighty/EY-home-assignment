app = 'family-tree-ui'
primary_region = 'iad'

[build]
  dockerfile = 'ui/Dockerfile'
  [build.args]
    NGINX_CONF = 'ui/nginx.fly.conf'

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0
